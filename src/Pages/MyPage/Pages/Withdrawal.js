import styled from 'styled-components'

function withdrawal() {
	return (
		<S.Wrapper>
			여기는 회원 탈퇴<div>(준비중)</div>
		</S.Wrapper>
	)
}
export default withdrawal

const Wrapper = styled.div`
	font-size: 40px;
	text-align: center;
`

const S = { Wrapper }
